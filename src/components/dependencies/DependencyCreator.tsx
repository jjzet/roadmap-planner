// This component is a placeholder for future visual feedback during dependency creation
// (e.g., a temporary line following the mouse from the source bar).
// Currently, the dependency creation mode is handled in TimelineBar click events
// and the UI state in uiStore.

export function DependencyCreator() {
  return null;
}
